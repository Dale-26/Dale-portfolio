# Copy this file to ".env" and paste your real key.
# Get a free Gemini API key at https://aistudio.google.com (no card required).
# IMPORTANT: no VITE_ prefix — this key stays server-side (in the /api proxy)
# and is never bundled into the browser.
GEMINI_API_KEY=your_key_here
